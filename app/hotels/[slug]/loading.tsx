import RouteDetailSkeleton from "@/components/RouteDetailSkeleton";

// Hotel detail page layout is similar enough to route detail that we can
// reuse the same skeleton — same overall structure (hero, grid, sections).
export default function Loading() {
  return <RouteDetailSkeleton />;
}
